# Optional. Get a free token (read access is enough) at https://huggingface.co/settings/tokens
# Removes the "unauthenticated requests" warning and gives higher download rate limits.
# Copy this file to .env and paste your token in — .env is git-ignored.
HF_TOKEN=
